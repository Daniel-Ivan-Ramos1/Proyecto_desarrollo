<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Proyecto_desarrollo.MainPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="TechStore - Inicio">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="30">

            <Frame BackgroundColor="#2E86AB" 
                   Padding="20" 
                   CornerRadius="15">
                <VerticalStackLayout>
                    <Label Text="🖥️ TechStore" 
                           FontSize="28" 
                           TextColor="White" 
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>
                    <Label Text="Tu tienda de computadoras" 
                           FontSize="16" 
                           TextColor="White"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Frame>

            <Frame BackgroundColor="#F0F8FF" Padding="20" CornerRadius="10">
                <Label Text="Bienvenido a la aplicación MAUI" 
                       FontSize="18" 
                       HorizontalOptions="Center"
                       TextColor="#2E86AB"/>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>