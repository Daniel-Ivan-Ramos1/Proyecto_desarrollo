<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Proyecto_desarrollo.Views.LoginPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="🔐 Admin TechStore"
             BackgroundColor="{DynamicResource BackgroundColor}">

    <ScrollView>
        <VerticalStackLayout Spacing="40" Padding="30" VerticalOptions="Center">
            <VerticalStackLayout Spacing="15" HorizontalOptions="Center">
                <Label x:Name="IconLabel"
                       Text="🔐" 
                       FontSize="48"
                       HorizontalOptions="Center"/>

                <Label Text="TechStore Admin" 
                       FontSize="28" 
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       TextColor="{DynamicResource PrimaryColor}"/>

                <Label Text="Panel de Administración" 
                       FontSize="16"
                       HorizontalOptions="Center"
                       TextColor="{DynamicResource TextColor}"/>
            </VerticalStackLayout>

            <Frame Style="{DynamicResource CardFrame}" 
                   HorizontalOptions="Center"
                   Padding="40">
                <VerticalStackLayout Spacing="20" HorizontalOptions="Center">

                    <Frame x:Name="FingerprintFrame"
                           BackgroundColor="{DynamicResource CardColor}"
                           CornerRadius="70"
                           WidthRequest="140"
                           HeightRequest="140"
                           Padding="0"
                           HasShadow="True">

                        <VerticalStackLayout Spacing="5" 
                                             HorizontalOptions="Center" 
                                             VerticalOptions="Center">
                            <Label x:Name="FingerprintIcon"
                                   Text="🔒"
                                   FontSize="48"
                                   HorizontalOptions="Center"/>

                            <Label x:Name="FingerprintText"
                                   Text="Mantener presionado"
                                   FontSize="10"
                                   HorizontalOptions="Center"/>

                            <ProgressBar x:Name="VerificationProgress"
                                         Progress="0"
                                         WidthRequest="100"
                                         HeightRequest="4"
                                         ProgressColor="{DynamicResource PrimaryColor}"
                                         IsVisible="False"/>
                        </VerticalStackLayout>
                    </Frame>

                    <Button x:Name="WindowsButton"
                            Text="👆 MANTENER PRESIONADO" 
                            BackgroundColor="#2E86AB"
                            TextColor="White"
                            CornerRadius="10"
                            FontSize="14"
                            Padding="40,20"
                            IsVisible="False"/>

                    <Label x:Name="StatusLabel" 
                           Text="Mantenga presionado para verificar"
                           FontSize="12"
                           HorizontalOptions="Center"
                           TextColor="{DynamicResource TextColor}"
                           HorizontalTextAlignment="Center"/>

                    <Label x:Name="TimerLabel"
                           Text=""
                           FontSize="10"
                           HorizontalOptions="Center"
                           TextColor="Gray"
                           IsVisible="False"/>

                </VerticalStackLayout>
            </Frame>

            <Frame Style="{DynamicResource CardFrame}" x:Name="InfoFrame">
                <VerticalStackLayout Spacing="10">
                    <Label x:Name="ModeTitleLabel" 
                           Text="💡 Modo: Detección Automática"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource TextColor}"/>
                    <Label x:Name="ModeDescriptionLabel"
                           Text="Sistema detectando plataforma..."
                           FontSize="12"
                           TextColor="{DynamicResource TextColor}"/>
                    <Label x:Name="ModeHintLabel"
                           Text=""
                           FontSize="10"
                           TextColor="#FF6B6B"/>
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>