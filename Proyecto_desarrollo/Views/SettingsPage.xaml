<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Proyecto_desarrollo.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="⚙️ Ajustes"
             BackgroundColor="{DynamicResource BackgroundColor}">

    <ContentPage.Content>
        <ScrollView>
            <VerticalStackLayout Spacing="25" Padding="20">

                <Label Text="Configuración" 
                       FontSize="24" 
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       TextColor="{DynamicResource TextColor}"/>

                <Frame Style="{DynamicResource CardFrame}">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="🎨 Tema de Colores" 
                               FontAttributes="Bold"
                               TextColor="{DynamicResource TextColor}"/>

                        <Button x:Name="TemaAzulButton"
                                Text="🔵 Tema Azul (Tech)" 
                                Style="{DynamicResource PrimaryButton}"
                                Clicked="OnTemaAzulClicked"/>

                        <Button x:Name="TemaOscuroButton"
                                Text="🌙 Tema Oscuro" 
                                Style="{DynamicResource PrimaryButton}"
                                Clicked="OnTemaOscuroClicked"/>
                    </VerticalStackLayout>
                </Frame>

                <Frame Style="{DynamicResource CardFrame}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="ℹ️ Acerca de" 
                               FontAttributes="Bold"
                               TextColor="{DynamicResource TextColor}"/>
                        <Label Text="🖥️ TechStore v1.0"
                               TextColor="{DynamicResource TextColor}"/>
                        <Label Text="Tu tienda especializada en computadoras"
                               TextColor="{DynamicResource TextColor}"/>
                        <Label Text="Desarrollado con .NET MAUI"
                               TextColor="{DynamicResource TextColor}"/>
                        <Label Text="✅ Proyecto funcionando correctamente"
                               TextColor="{DynamicResource TextColor}"/>
                    </VerticalStackLayout>
                </Frame>

                <Button Text="🔄 Recargar Datos" 
                        BackgroundColor="{DynamicResource AccentColor}"
                        TextColor="White"
                        CornerRadius="10"
                        Clicked="OnRecargarDatosClicked"/>

            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>